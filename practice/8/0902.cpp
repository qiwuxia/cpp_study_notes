#include <iostream>
#include <deque>
#include <vector>
#include <list>

using namespace std;


int main()
{
    list<deque<int>> lis_de;
    vector<int> i;
    if (i.begin() == i.end())
        cout << "空迭代器" << endl;
    return 0;
}